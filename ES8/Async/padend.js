// padEnd()

// Syntax:
// string.padEnd(targetLength, padString)

// Example:
let str = "5";
let paddedEnd = str.padEnd(3, "0");
console.log(paddedEnd); // Output: "50"
// Explanation:
// In this case, "5" is padded with "0" from the end to make it 3 characters long. Thus, the output is "50".